<div class="color-line">
</div>
<div id="logo" class="light-version">
    <a href="@Url.Action("Index", "Main")">
        <span class="nav-label">
            @if (!string.IsNullOrEmpty(@ViewBag.tipo))
            {@ViewBag.tipo.ToUpper()}
        </span>
    </a>
    <a href="main.aspx"><span></span></a>
</div>
<nav role="navigation">
    <div class="header-link hide-menu"><i class="fa fa-bars"></i></div>
    <div class="small-logo">
        <a href="@Url.Action("Index", "Main")">
            <span class="text-primary">
                @if (!string.IsNullOrEmpty(@ViewBag.tipo))
            {@ViewBag.tipo.ToUpper()}
        </span>
    </a>
</div>
             <div class="pull-left" style="margin-top: 1rem;">
	             <select class="form-control" ng-options="equipo as equipo.categoria for equipo in vm.equipos" ng-model="vm.equipo" ng-change="vm.changeEquipo()"></select>
             </div>
<div class="mobile-menu">
    <button type="button" class="navbar-toggle mobile-menu-toggle" data-toggle="collapse" data-target="#mobile-collapse">
        <i class="fa fa-chevron-down"></i>
    </button>
    <div class="collapse mobile-navbar" id="mobile-collapse">
        <ul class="nav navbar-nav">
            @*<li>
                    <a class="" href="#">Link 1</a>
                </li>*@
            <li>
                <a href="@Url.Action("Logout", "Account")">
                    <i class="pe-7s-power m-r-xs"></i><span>Salir</span>
                </a>
            </li>
        </ul>
    </div>
</div>
<div class="navbar-right">
    <ul class="nav navbar-nav no-borders">
        <li>
            <a href="#" id="sidebar" class="right-sidebar-toggle">
                <i class="pe-7s-upload pe-7s-news-paper"></i>
            </a>
        </li>
        <li>
            <a href="@Url.Action("Logout", "Account")" tooltip-placement="bottom" tooltip="Salir">
                <i class="pe-7s-power"></i>
            </a>
        </li>
    </ul>
</div>
</nav>
